subprojects {
    apply plugin: "groovy"

    dependencies {
        compile localGroovy()
        compile gradleApi()
    }

    rootProject.dependencies {
        println 'path='+path
        runtime project(path)
    }
}
